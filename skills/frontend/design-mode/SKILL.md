---
name: design-mode
description: Use when the user asks to make UI or design changes and verify them in a browser — e.g. "change this layout", "tweak the design", "update the styling", "make it look better" and the change should be visually checked in the running app.
---

# Design Mode

Perform the requested UI/design changes, then verify them in a browser. Assume the application is already running.
