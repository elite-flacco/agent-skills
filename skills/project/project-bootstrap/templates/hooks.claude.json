{
  "hooks": {
    "SubagentStop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "git diff --quiet HEAD || npm run verify || exit 2",
            "timeout": 180000
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "git diff --quiet HEAD || npm run verify || exit 2",
            "timeout": 180000
          }
        ]
      }
    ]
  }
}
